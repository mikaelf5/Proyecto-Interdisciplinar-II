from django.apps import AppConfig


class CirculacionConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.circulacion"
    verbose_name = "Préstamos / Circulación"